# Changelog

Історія змін ARTLINE Rich Studio. Поточна версія — `apps/api/app/version.py`.

## v12.0 — інженерний фундамент

- Єдине джерело версії — `apps/api/app/version.py`; health-ендпоінт і теги образів беруться звідти.
- CI + реєстр образів (GHCR): GitHub Actions на кожен push у `main` проганяє перевірки й pytest, збирає та пушить образи `rich-ai-api` і `rich-ai-web`.
- Деплой через `pull` (`docker-compose.registry.yml`) без збирання на сервері — див. [DEPLOY.md](DEPLOY.md).
- Стійкість пайплайна: виклики OpenAI обгорнуто ретраями з експоненційним backoff.
- Адаптація до reasoning-моделей: більший бюджет виводу і керований `reasoning.effort`, бо приховані reasoning-токени списуються з `max_output_tokens`.
- Покращено витягування характеристик: парсинг таблиць, `<dl>` і spec-блоків прямо з HTML + злиття з JSON-LD.
- Захист від неповних сторінок: якщо модель повернула лише Hero — повторна спроба, далі відкат на повний шаблон.
- Промпт `ARTLINE Base` більше не допускає мета-тексту про сторінку та зображення.
- Каркас міграцій Alembic (`apps/api/alembic/`) — опційний, з безпечним переходом поверх наявної схеми.
- Ліцензія: PolyForm Noncommercial 1.0.0.

## v11.10

- Явний вибір текстової моделі у діалозі створення; підказки до полів.
- Блок мов: Українська та Російська за замовчуванням, Польська швидким чекбоксом, решта — через пошук.
- Прибрано «мигання» інтерфейсу: перемальовування лише коли дані реально змінилися.
- Детальний розрахунок вартості за етапами (аналіз товару, текст, зображення).
- Живий таймер часу виконання проєкту.
- Дисклеймер про евристичну природу AI-перевірок якості.
- Артикул (SKU) окремим блоком та у пошуку; робочий блок «Категорія».

## v11.9

- Видалення стилів; `ARTLINE Base` захищений, проєкти переприв’язуються до базового.
- Промпт `ARTLINE Base`: обов’язковий `alt`, контраст за WCAG AA, ліміт абзаців, надійніша мобільна Hero-верстка, заборона вигаданих фактів заради кількості карток.

## v11.8

- Безпека: санітизація всього rich-HTML (XSS), захист від SSRF, rate-limit входу, захист останнього адміністратора, перехід на `PyJWT`.
- Пауза та скасування реально зупиняють worker; повторний запуск блокується і в стані `queued`.
- Видалення проєкту, звіти AI-критики в UI, кнопки керування чергою.
- Прибрано мертвий код: brands, knowledge, playground, compare, workflows, publish targets, benchmark, analytics.

## v11.7

- Єдина система контентних «острівців»: текст завжди на узгоджених поверхнях.
- Компактний напівпрозорий темний острівець під Hero-текстом.
- Прибрані голі текстові колонки та надмірні порожні проміжки.

## v11.6

- Оновлений керований `ARTLINE Base`: єдиний дизайн-код, SEO/GEO-структура, product-in-use Hero та Feature.
- Гнучкий вибір до 10 мов; медіатека з пошуком, фільтрами й сортуванням.
- Сумісність `gpt-image-2-*`: `input_fidelity` не надсилається.

## v11.5

- Системний стиль `ARTLINE Base`; назва стилю у списку та робочій області.
- Діалог вибору стилю при повторній генерації; історія HTML-версій.
- Автоматичне оновлення PostgreSQL enum значеннями Review.
- Експорт проєкту у ZIP; автозбереження редактора стилю.

## v11.4

- Один майстер-макет на viewport; інші мови — переклад лише текстових вузлів.
- Окремі Hero для Desktop (`1536×1024`) і Mobile (`1024×1536`).
- Світле полотно перегляду; режими карток і списку на сторінці проєктів.

## v11.3

- Визначення реального фото товару з `Product.image`, галереї, `srcset` і zoom-посилань.
- Об’єднання варіантів 220/600/1400/original одного кадру; прибрано хибне обмеження 20 КБ.
- Відновлення некоректного JSON-LD; локальний перевірений референс `product-reference.png`.
- Advisory lock при ініціалізації PostgreSQL; блокування одночасних записів HTML-версій.
- Rich-контент ніколи не містить `<h1>`.
